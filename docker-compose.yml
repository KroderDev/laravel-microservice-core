services:
  composer:
    image: composer:latest
    working_dir: /app
    volumes:
      - .:/app

  php:
    image: composer:latest
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["php"]
